# services/media (placeholder)

Future home of media library business logic (`MediaAsset`, `MediaVersion`,
review/approval, versioning). Will depend on `lib/storage` (the storage
abstraction), never on a specific storage provider SDK directly. Not
implemented in Phase 0 — see docs/roadmap.md.
