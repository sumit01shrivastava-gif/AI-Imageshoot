# app/components

Shared UI components go here once there is UI to share. Route-specific
markup lives directly in its route module under `app/routes/`; promote a
piece to this directory only once a second route needs it.

Empty in Phase 0 — no product-feature UI has been built yet (see
docs/roadmap.md).
